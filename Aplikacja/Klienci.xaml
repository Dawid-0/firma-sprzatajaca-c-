<Window x:Class="Aplikacja.Klienci"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Aplikacja"
        mc:Ignorable="d"
        Title="Klienci" Height="500" Width="850"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Grid Background="#F5F6F8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Wróć" Width="120" Height="30" 
                HorizontalAlignment="Left" Margin="15,15,0,0" 
                Background="#E0E0E0" BorderBrush="#BDBDBD" 
                Click="Wroc_Click" />

        <Border Grid.Row="1" Background="#5448D6" CornerRadius="15" Margin="15,15,15,15" Height="70">
            <TextBlock Text="Zarządzanie klientami" 
                       Foreground="White" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"/>
        </Border>

        <DataGrid Grid.Row="2" x:Name="KlienciDataGrid" 
                  AutoGenerateColumns="False" 
                  Margin="15,0,15,15" 
                  CanUserAddRows="False" 
                  Background="White"
                  BorderBrush="#D6D6D6"
                  BorderThickness="1"
                  RowHeight="30"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Imię" Binding="{Binding Imie}" Width="*"/>
                <DataGridTextColumn Header="Nazwisko" Binding="{Binding Nazwisko}" Width="*"/>
                <DataGridTextColumn Header="Adres" Binding="{Binding Adres}" Width="*"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Telefon}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Margin="15,0,15,15" Background="White" HorizontalAlignment="Stretch">

            <Grid HorizontalAlignment="Center" Margin="0,15,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" Text="Imię" Margin="0,0,0,5" Foreground="#333"/>
                <TextBlock Grid.Column="2" Grid.Row="0" Text="Nazwisko" Margin="0,0,0,5" Foreground="#333"/>
                <TextBlock Grid.Column="4" Grid.Row="0" Text="Adres" Margin="0,0,0,5" Foreground="#333"/>
                <TextBlock Grid.Column="6" Grid.Row="0" Text="Telefon" Margin="0,0,0,5" Foreground="#333"/>

                <TextBox Grid.Column="0" Grid.Row="1" x:Name="ImieTextBox" Height="30" Padding="5" BorderBrush="#CCC"/>
                <TextBox Grid.Column="2" Grid.Row="1" x:Name="NazwiskoTextBox" Height="30" Padding="5" BorderBrush="#CCC"/>
                <TextBox Grid.Column="4" Grid.Row="1" x:Name="AdresTextBox" Height="30" Padding="5" BorderBrush="#CCC"/>
                <TextBox Grid.Column="6" Grid.Row="1" x:Name="TelefonTextBox" Height="30" Padding="5" BorderBrush="#CCC"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                <Button Content="Dodaj" Width="120" Height="30" Margin="0,0,15,0" Background="#E0E0E0" BorderBrush="#BDBDBD" Click="Dodaj_Click"/>
                <Button Content="Usuń" Width="120" Height="30" Background="#E0E0E0" BorderBrush="#BDBDBD" Click="Usun_Click"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>